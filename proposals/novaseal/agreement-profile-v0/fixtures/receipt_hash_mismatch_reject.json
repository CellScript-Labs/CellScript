{
  "schema": "novaseal-agreement-fixture-v0.1",
  "fixture": "receipt_hash_mismatch_reject",
  "action": "originate_agreement / terminal paths",
  "expected": "rejected",
  "coverage": "action-ckb-vm-covered + resolved-transaction-covered",
  "reject_guard": "materialized NovaAgreementReceiptV0.new_receipt_root must equal the witness receipt_hash",
  "description": "A terminal transaction whose materialized receipt output carries a different new_receipt_root than the witness receipt_hash must reject."
}
