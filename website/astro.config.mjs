import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://a19q3.github.io",
  base: "/CellScript/",
  output: "static",
  devToolbar: {
    enabled: false,
  },
});
