// audit: phase=reject_compile
// audit: contains=undefined variable
// audit: contains=transfer
module cellscript::audit::seed_obsolete_transfer_keyword

resource Coin has store, transfer, destroy {
    amount: u64,
    nonce: u64,
}

action bad(coin: Coin, to: Address) -> next_coin: Coin
where
    transfer coin to to
