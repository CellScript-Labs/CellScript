// audit: phase=reject_compile
// audit: contains=cannot store a cell-backed resource
module cellscript::audit::seed_bounded_collection_vec_resource_reject

resource Token has store, consume {
    amount: u64,
}

action batch(tokens: Vec<Token>) -> u64 {
    verification
        return 0
}
