// audit: phase=reject_compile
// audit: contains=error
// audit: description="legacy 'has transfer' and 'transfer X to Y' are no longer valid grammar (0.19 cleanup)"
module cellscript::audit::seed_legacy_transfer_capability

resource Coin has store, transfer {
    amount: u64,
    nonce: u64,
}

action bad(coin: Coin, to: Address) {
    verification
        transfer coin to to
}
